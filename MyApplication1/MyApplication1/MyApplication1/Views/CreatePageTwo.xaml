<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MyApplication1.Views.CreatePageTwo"
             Title="Anzeige erstellen">
  <ContentPage.Content>
    <StackLayout>
      <Button Text="Bild aufnehmen" Command="{Binding TakePictureCommand}" />
      <Button Text="Bild hochladen" Command="{Binding SelectPictureCommand}" />
      <Button Text="Video hochladen" Command="{Binding SelectVideoCommand}" />
      <Image Source="{Binding ImageSource}" VerticalOptions="CenterAndExpand" />
      <Entry Text="{Binding VideoInfo}" VerticalOptions="CenterAndExpand" />
      <Button Text="Weiter"
        HorizontalOptions="Center"
        VerticalOptions="CenterAndExpand"
        Clicked="OnButtonClickedContinue" />
    </StackLayout>
  </ContentPage.Content>
</ContentPage>